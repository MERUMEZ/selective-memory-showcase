# Сама память ставится пакетом: витрина — её ПОТРЕБИТЕЛЬ, а не копия.
# Проверено на чистом окружении: pip install selective-memory + этот файл,
# и тесты витрины проходят без единого файла библиотеки в репозитории.
selective-memory>=0.1.0

python-dotenv>=1.0.0
requests>=2.31.0
aiogram>=3.4.1
pytest>=7.4.0

# Семантический поиск по памяти (services/embeddings.py).
# Модель navec (~51 МБ) кладётся в storage/models/ отдельно — см. deploy/README.md.
# Без неё поиск молча деградирует до строкового, бот продолжает работать.
navec>=0.10.0
numpy>=1.24.0
